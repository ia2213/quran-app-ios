name: mural_voice_ai
description: "Mural Voice AI - Assistant Vocal Allemand avec Hermes VPS, Gemini & Groq"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_tts: ^4.2.0
  record: ^5.1.2
  path_provider: ^2.1.4
  just_audio: ^0.9.36
  audio_session: ^0.1.21
  url_launcher: ^6.3.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
